<%= simple_form_for(@user) do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> malogró todo:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :name %>
  </div>

  <div class="field">
    <%= f.input :credits %>
  </div>

  <div class="field">
    <%= f.association :books, :as => :check_boxes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
